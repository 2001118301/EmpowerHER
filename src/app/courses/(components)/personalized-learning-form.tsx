
// This component is no longer needed as its functionality has been integrated 
// directly into /src/app/courses/page.tsx for a multi-step questionnaire.
// Keeping the file to avoid breaking imports if any were missed, but it should be deleted.

export function PersonalizedLearningForm() {
  return null; 
}
